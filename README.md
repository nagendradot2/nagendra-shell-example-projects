This file will input from the user has arguments and will create a user with a user access expiry datails 
if wrong argumensts are passed it returns the error
after runnig this file use below commands to check the output of the scripts if users are created or not 
grep username /etc/passwd
